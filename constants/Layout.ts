export const LAYOUT = {
  explorerWidth: 250,
  tocWidth: 220,
  middlePaneWidth: 300,
  footerHeight: 250,
  minPaneWidth: 150,
  maxPaneWidth: 800,
  minFooterHeight: 100,
  maxFooterHeight: 800,
} as const;

export const DELAYS = {
  contentUpdate: 300,
} as const;
